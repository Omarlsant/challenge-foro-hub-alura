package com.challengeforohub.Foro.Hub.domain.topico.validations.create;

import com.challengeforohub.Foro.Hub.domain.topico.dto.CrearTopicoDTO;

public interface ValidarTopicoCreado {
    void validate(CrearTopicoDTO data);
}
